# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUI-Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI-Weather

pod 'Alamofire', '5.3'

  target 'SwiftUI-WeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUI-WeatherUITests' do
    # Pods for testing
  end

end
